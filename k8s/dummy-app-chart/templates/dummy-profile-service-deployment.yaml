apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.dummy-profile-service.name }}
  namespace: {{ .Values.namespace }}
  {{- with .Values.dummy-profile-service.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.dummy-profile-service.replicaCount }}
  selector:
    {{- with .Values.dummy-profile-service.labels }}
    matchLabels:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  template:
    metadata:
      {{- with .Values.dummy-profile-service.labels }}
      labels:
        {{- toYaml . | nindent 8 }}
      {{- end }}

    spec:
      containers:
        - name: {{ .Values.dummy-profile-service.name }}
          image: {{ .Values.dummy-profile-service.image }}
          ports:
            - name: http
              containerPort: {{ .Values.dummy-profile-service.port }}
              protocol: TCP