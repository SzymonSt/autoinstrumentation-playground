apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.dummy-score-service.name }}-service
  namespace: {{ .Values.namespace }}
  {{- with .Values.dummy-score-service.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.dummy-score-service.port }}
      targetPort: {{ .Values.dummy-score-service.port }}
      protocol: TCP
  {{- with .Values.dummy-score-service.labels }}
  selector:
    {{- toYaml . | nindent 4 }}
  {{- end }}
