apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.dummy-profile-service.name }}-service
  namespace: {{ .Values.namespace }}
  {{- with .Values.dummy-profile-service.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.dummy-profile-service.port }}
      targetPort: {{ .Values.dummy-profile-service.port }}
      protocol: TCP
  {{- with .Values.dummy-profile-service.labels }}
  selector:
    {{- toYaml . | nindent 4 }}
  {{- end }}
